name: rive_canvas
description: This package helps rendering Rives on a canvas directly. 
  It also comes with a RiveComponent for the Flame game engine.
version: 0.0.4
homepage: 'https://www.brachy84.me'
repository: 'https://github.com/brachy84/rive_canvas'
issue_tracker: 'https://github.com/brachy84/rive_canvas/issues'

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^0.29.0
  rive: ^0.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: null
